## RPI
# ssh
ssh utrapi
# check motor control
systemctl status motor_control.service

## running the rover
# build (make sure to conda deactivate)
build

# sim
ros2 launch timbot_launch timbot.launch.py config:=sim.yaml

# comp (have all the sensors conencted through the usb hub)
ros2 launch timbot_launch timbot.launch.py config:=comp.yaml

# if smth doesn't work, then update the hardware ports in the comp.yaml 
# figure out the ports by running the commands under the ports in the yaml file